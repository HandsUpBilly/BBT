{
  "name": "bloodbowl",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "npm run generate:seed && cd client && npm run build",
    "generate:seed": "node scripts/generate-scenario-seed.mjs",
    "start": "cd server && node index.js",
    "lint": "cd client && npm run lint",
    "test": "node --test shared/*.test.js netlify/tests/*.test.js && cd client && npm test",
    "check:functions": "node scripts/check-function-bundles.mjs",
    "verify": "npm run lint && npm test && npm run build && npm run check:functions"
  }
}
